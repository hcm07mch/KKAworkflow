/**
 * Supabase Database Types
 *
 * 이 파일은 `supabase gen types typescript` 명령으로 자동생성됩니다.
 * 아직 생성되지 않은 경우 임시 플레이스홀더를 사용합니다.
 */

// TODO: `npx supabase gen types typescript --local` 으로 실제 타입 생성
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;

